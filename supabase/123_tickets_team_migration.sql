-- ============================================
-- 인입내역 담당팀 컬럼 추가
-- 새 인입내역 등록 시 CS팀 / 기술지원팀 중 하나를 버튼으로 선택해 저장한다.
-- 상태(status) 흐름은 그대로 두고 담당팀만 별도로 기록한다.
-- 기존 행은 NULL로 남으며 화면에서는 뱃지를 표시하지 않는다.
-- ============================================

ALTER TABLE tickets
  ADD COLUMN IF NOT EXISTS team TEXT CHECK (team IN ('cs', 'tech'));
